{
  "name": "how-react-works",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/node": "^18.6.3",
    "@types/react": "17.0.2",
    "@types/react-dom": "17.0.2",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "build:react": "cd react && yarn install && yarn run build",
    "build:copy": "node ./tools/index.js",
    "test": "react-app-rewired test --env=jsdom"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "customize-cra": "^1.0.0",
    "fs-extra": "^10.1.0",
    "react-app-rewired": "^2.2.1",
    "typescript": "^4.7.4"
  }
}
